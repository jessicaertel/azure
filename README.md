# azure
1. **Instale o EB CLI** (se ainda não tiver):

2. **Inicialize o Elastic Beanstalk** no diretório do seu projeto:

3. **Crie o ambiente**:

4. **Faça o deploy da aplicação**:

Além disso, crie um arquivo `requirements.txt` com as dependências do Flask:

Certifique-se de ter o arquivo `application.py` e `requirements.txt` no mesmo diretório antes de rodar os comandos do EB CLI.
 🚀💻✨
